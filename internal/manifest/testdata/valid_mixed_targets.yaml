apiVersion: kubolt.io/v1
apps:
  - name: walls
    chartPath: ./apps/walls
    namespace: walls
    backup:
      targets:
        - type: filesystem
          pvc: walls-data
        - type: pg_dump
          podSelector: app=postgres
